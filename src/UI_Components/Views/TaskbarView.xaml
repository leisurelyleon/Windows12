<UserControl
    x:Class="Windows12.UI_Components.Views.TaskbarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    PointerPressed="OnTaskbarPointerPressed"
    PointerMoved="OnTaskbarPointerMoved"
    PointerReleased="OnTaskbarPointerReleased">

    <Border Background="#CC222222" 
            CornerRadius="{StaticResource FloatingShellCornerRadius}" 
            BorderThickness="1" 
            BorderBrush="#44FFFFFF"
            Padding="12,0,12,0">
        <Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="16">
                <Button Width="48" Height="48" Content="Core" CornerRadius="8"/>
                <Button Width="48" Height="48" Content="Dev" CornerRadius="8"/>
                <Button Width="48" Height="48" Content="Files" CornerRadius="8"/>
                <Button Width="48" Height="48" Content="Terminal" CornerRadius="8"/>
                <Button Width="48" Height="48" Content="Settings" CornerRadius="8"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="144Hz // Secured" Foreground="{StaticResource TextHighContrastBrush}" FontSize="12"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
