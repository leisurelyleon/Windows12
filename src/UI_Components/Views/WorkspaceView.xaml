<UserControl
    x:Class="Windows12.UI_Components.Views.WorkspaceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{StaticResource DarkCarbonBackgroundBrush}" 
            CornerRadius="{StaticResource FloatingShellCornerRadius}"
            Padding="24"
            BorderThickness="1.5"
            BorderBrush="{StaticResource DarkCarbonComponentBrush}">
        <StackPanel Spacing="20">
            <TextBlock Text="DEV WORKSPACE CORE" Foreground="{StaticResource TextHighContrastBrush}" FontSize="18" FontWeight="Bold"/>
            
            <Border Background="{StaticResource DarkCarbonComponentBrush}" Padding="16" CornerRadius="8">
                <StackPanel Spacing="8">
                    <ToggleSwitch x:Name="DevModeToggleControl" 
                                  Header="Microsoft Dev Mode Isolation" 
                                  Foreground="{StaticResource TextHighContrastBrush}"
                                  Toggled="OnDevModeToggled"/>
                    <TextBlock Text="Suppresses tracking telemetry services, routes diagnostic servers to localhost loopback, and locks compiler cache allocation heaps." 
                               Foreground="#AAAAAA" 
                               FontSize="11" 
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource DarkCarbonComponentBrush}" Padding="16" CornerRadius="8">
                <StackPanel Spacing="8">
                    <TextBlock Text="Hardware Performance Allocation" Foreground="{StaticResource TextHighContrastBrush}" FontSize="12"/>
                    <ProgressBar Value="100" Maximum="100" IsIndeterminate="False"/>
                    <TextBlock Text="Status: 100% Core Focus Mode Engaged" Foreground="#FFD166" FontSize="11"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
